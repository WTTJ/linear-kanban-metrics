# Linear Kanban Metrics - Environment Configuration
# Copy this file to .env and fill in your actual values

# Required Variables
LINEAR_API_TOKEN=your_linear_api_token_here
# Linear API token for accessing Linear API
# Get your token from: https://linear.app/settings/api

# GitHub Configuration (Required for PR operations)
GITHUB_REPOSITORY=your-org/your-repo
# GitHub repository in format: owner/repository-name

GITHUB_TOKEN=your_github_token_here
# GitHub personal access token for API access
# Get from: https://github.com/settings/tokens

PR_NUMBER=123
# Pull request number for PR-related operations

# API Provider Selection (Required for AI operations)
API_PROVIDER=dust
# Choose either "anthropic" or "dust"
# Default: dust

# Anthropic API Configuration (Required when API_PROVIDER=anthropic)
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Anthropic API key for Claude models
# Get from: https://console.anthropic.com/

# Dust API Configuration (Required when API_PROVIDER=dust)
DUST_API_KEY=your_dust_api_key_here
# Dust API key for AI operations

DUST_WORKSPACE_ID=your_dust_workspace_id_here
# Dust workspace identifier

DUST_AGENT_ID=your_dust_agent_configuration_id_here
# Dust agent configuration ID

# Optional Variables
LINEAR_TEAM_ID=
# Linear Team ID - can be specified to limit queries to specific team
# Leave empty to fetch from all teams
# Format: team-key or team-id

METRICS_START_DATE=
# Start date for metrics collection (ISO 8601 format: YYYY-MM-DD)
# If not specified, defaults to 30 days ago
# Example: 2024-01-01

METRICS_END_DATE=
# End date for metrics collection (ISO 8601 format: YYYY-MM-DD)  
# If not specified, defaults to today
# Example: 2024-12-31

# Development and Debugging
DEBUG=
# Set to any value to enable debug logging
# Example: DEBUG=true

QUIET=
# Set to any value to suppress non-essential output
# Example: QUIET=true

# Cache Configuration
CACHE_TTL_HOURS=24
# Cache TTL in hours (default: 24 hours)
# Set to 0 to disable caching

CACHE_DIR=tmp/.linear_cache
# Cache directory (default: tmp/.linear_cache)
# Can be customized for different environments

# Notes:
# - LINEAR_API_TOKEN is required for all functionality
# - Optional variables can be left empty to use defaults
# - Never commit .env files to version control
# - Run './scripts/env-handler.sh' to generate from 1Password template