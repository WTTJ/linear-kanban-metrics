# Linear Kanban Metrics - Environment Configuration Template
# This file contains 1Password references that will be resolved by scripts/env-handler.sh

# Required Variables
# Linear API token for accessing Linear API
LINEAR_API_TOKEN=op://Employee/LINEAR_API_TOKEN/credential

# GitHub Configuration (Required for PR operations)
#GITHUB_REPOSITORY=
GITHUB_TOKEN=op://Employee/GitHub Personal Access Token/token
#PR_NUMBER=

# API Provider Selection (Required for AI operations)
# Choose either "anthropic" or "dust"
API_PROVIDER=dust

# Anthropic API Configuration (Required when API_PROVIDER=anthropic)
ANTHROPIC_API_KEY=op://Employee/ANTHROPIC_API_KEY/credential

# Dust API Configuration (Required when API_PROVIDER=dust)
DUST_API_KEY=op://Employee/DUST/DUST_API_KEY
DUST_WORKSPACE_ID=op://Employee/DUST/DUST_WORKSPACE_ID
DUST_AGENT_ID=op://Employee/DUST/DUST_AGENT_ID

# Optional Variables
# Linear Team ID - can be specified to limit queries to specific team
# Leave empty to fetch from all teams
#LINEAR_TEAM_ID=

# Metrics date range (ISO 8601 format: YYYY-MM-DD)
# If not specified, defaults to last 30 days
#METRICS_START_DATE=
#METRICS_END_DATE=

# Debug mode - set to any value to enable debug logging
#DEBUG=

# Quiet mode - set to any value to suppress non-essential output
#QUIET=

# Cache TTL in hours (default: 24 hours)
CACHE_TTL_HOURS=24

# Cache directory (default: tmp/.linear_cache)
CACHE_DIR=tmp/.linear_cache

# Notes:
# - Use 'op://vault/item/field' format for 1Password references
# - Static values are copied as-is to .env
# - Run 'ruby scripts/env-handler.sh' to generate .env from this template
# - LINEAR_API_TOKEN is required - get yours from https://linear.app/settings/api
# - Optional variables can be left empty to use defaults
# - Never commit .env files to version control